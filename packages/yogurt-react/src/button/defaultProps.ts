import { YButtonProps } from './type';

const defaultProps: YButtonProps = {
  size: 'medium',
  variant: 'outlined',
  shape: 'rectangle',
  style: {},
  className: '',
  prefix: null,
  suffix: null,
  children: null,
};

export default defaultProps;
