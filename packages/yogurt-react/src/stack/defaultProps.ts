import { YStackProps } from './type';

const defaultProps: YStackProps = {
  space: 5,
  align: 'flex-start',
  justify: 'flex-start',
  direction: 'row',
  style: {},
  className: '',
  children: null,
};

export default defaultProps;
