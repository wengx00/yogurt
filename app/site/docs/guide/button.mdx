# Button 按钮

> 可以点击的按钮

```tsx
import { Button } from '@yogurt-ui/react';

export default function App() {
  return (
    <div style={{ display: 'flex', gap: 10 }}>
      <Button>Rectangle按钮</Button>
      <Button shape="round">Round按钮</Button>
    </div>
  )
}
```